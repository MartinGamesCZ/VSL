declare printf(string): int;
declare system(string): int;

fun print(text: string): void {
    printf(text);
}

fun println(text: string): void {
    print(text);
    print("\n");
}

fun executeCommand(command: string): void {
    system(command);
}

fun clearConsole(): void {
    executeCommand("clear");
}
